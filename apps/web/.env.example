# Frontend env — add these in your Vercel project settings
# (NOT committed to git — this is just a reference)

# Your Render backend URL (no trailing slash)
NEXT_PUBLIC_API_URL=https://YOUR-BACKEND.onrender.com
